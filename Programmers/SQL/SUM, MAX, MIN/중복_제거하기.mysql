/*
    [다시 풀었을 때 헷갈리는 부분]
    DISTINCT와 GROUP BY의 차이?
    DISTINCT : 주로 UNIQUE한 컬럼이나 튜플을 조회하는 경우 
    GROUP BY : 데이터를 그룹핑해서 그 결과를 가져오는 경우 (정렬)

    COUNT()를 해야 하는 경우, GROUP BY를 사용하면 서브 쿼리를 작성해야 함
    따라서 DISTINCT를 사용하는 것이 좋음

    집합함수(Aggregation)가 필요한 경우, GROUP BY를 사용하는 것이 좋음
*/

SELECT COUNT(DISTINCT(NAME))
FROM ANIMAL_INS